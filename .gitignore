# Disposable SQLite cache — rebuilt from posts/, never authoritative.
index.sqlite

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# uv-managed virtualenv (uv.lock is committed; the venv is not)
.venv/
